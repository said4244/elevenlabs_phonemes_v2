name: elevenlabs_phonemes_example
description: Example app for elevenlabs_phonemes Flutter package
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  elevenlabs_phonemes:
    path: ../
  
  rive: ^0.14.0-dev.14

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/charachter.riv

